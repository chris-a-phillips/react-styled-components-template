import React from 'react';
import { NavContainer } from './SCNavBar';

const NavBar = () => {
    return (
        <NavContainer>
            <h1>NavBar</h1>
        </NavContainer>
    );
};

export default NavBar;