import React from 'react';
import { NavWrapper } from './SCNavBar';

const NavBar = () => {
    return (
        <NavWrapper>
            <h1>NavBar</h1>
        </NavWrapper>
    );
};

export default NavBar;