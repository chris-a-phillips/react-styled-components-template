import React from 'react';
import { HeaderContainer } from '../../AppStyles';


const Header = () => {
    return (
        <HeaderContainer>
            <h1>Header</h1>
        </HeaderContainer>
    );
};

export default Header;